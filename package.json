{
  "name": "carmoneypit-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E and content-quality suite for CarMoneyPit",
  "scripts": {
    "pw:install": "npx playwright install chromium",
    "pw:build-jar": "gradlew.bat bootJar -x test --no-daemon",
    "pw:test": "npx playwright test",
    "pw:test:headed": "npx playwright test --headed",
    "pw:test:managed": "gradlew.bat clean bootJar -x test --no-daemon && set PW_USE_WEBSERVER=1&& npx playwright test",
    "pw:report": "npx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
